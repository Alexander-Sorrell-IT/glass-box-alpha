# Glass-Box Alpha — Claims Ledger (honesty source of truth)

Every line of submission/marketing copy must trace to a LIVE_ONCHAIN or BUILT_TESTED row here. DESIGNED_PENDING = phrase as design/future. DO_NOT_CLAIM = never say.

## Headline truths (lead with these)
- Three-way canonical-JSON keccak parity is CONFIRMED LIVE ON-CHAIN: calling verify() on the deployed Mantle Sepolia anchor recomputes the exact golden hash (0xf8aed1ad...82a) that the TypeScript SDK and the Python agent code produce independently — TS == Python == Solidity, proven on-chain, not just asserted in tests.
- The in-browser tamper test reads a GENUINE on-chain commit: VerifyPanel does a live getCommit(1,0) on chain 5003 and recomputes keccak256 of the canonical receipt in the browser to the byte-identical 0xfedc499e... — this is real cryptographic verification against a deployed contract, not a local mirror.
- Honest, reproducible backtest result framed as depth: the Fold beats the AVERAGE single agent's Sharpe 200/200 seeds and the WORST single agent's drawdown 200/200 (I reproduced both), and the code EXPLICITLY disclaims beating a plain mean — the measured edge is variance reduction only, and on direction the Fold ties a plain mean. The discipline of refusing the overclaim is itself the technical-depth signal.
- 148 passing automated tests across three independent stacks (59 Solidity/Foundry with fuzzing, 74 Python/pytest, 15 TypeScript/vitest), including a deliberate regression guard that the reasoning hash is Ethereum keccak256 and NOT FIPS sha3 — a subtle correctness trap most teams miss — plus golden vectors pinning the provenance-tag format and the arena scoring rule byte-for-byte across Python/TypeScript and Solidity.
- Five purpose-built contracts deployed and live on Mantle Sepolia (anchor, registry, round-state, non-transferable GBRR reputation token, human arena), plus a written-and-tested risk engine (AgentExecutor: 5% size cap, 50% confidence floor, 20% drawdown halt, Devil's-Advocate veto) and a Merchant Moe LB adapter — built and tested, not yet deployed.

## Ledger

| Claim | Tag | Evidence |
|---|---|---|
| ReasoningHashAnchor is deployed and live on Mantle Sepolia (chain 5003) at 0xB0319b2e88d95B2d7Ce706feC7E2799d9b93353d. It anchors keccak256 reasoning-receipt hashes via commit(agentId,decisionIndex,hash) and recomputes the hash on-chain via verify() for the browser tamper test. | `LIVE_ONCHAIN` | eth_getCode bytecode present (1649 bytes) + my own cast call commitsCount()=2 on chain 5003 via rpc.sepolia.mantle.xyz; src/ReasoningHashAnchor.sol; 7 passing Foundry tests incl testFuzz_CommitRoundTrip (256 runs) |
| Exactly 2 reasoning commits are live on-chain, both agentId=1 (Chronos): commits[0]=(1,17,0x91349ba4...), commits[1]=(1,0,0xfedc499e...). This is a single-agent anchor, NOT a four-agent anchor. | `LIVE_ONCHAIN` | my cast call commitsCount()=2; getCommit(1,0)=0xfedc499efb7e22a4050c72b80e5cf84a04e64afab3bf8d605dacad3b5dedbd33; getCommit(2,0)/(3,0)/(4,0) revert 'no commit' on chain 5003 |
| The in-browser VerifyPanel tamper test reads a GENUINE on-chain commit (live getCommit(1,0) on chain 5003) and recomputes keccak256 of the canonical receipt in the browser to the same byte-identical value (0xfedc499e...). | `LIVE_ONCHAIN` | VerifyPanel.tsx L40-52 useReadContract getCommit chainId 5003 (onChainHash ?? COMMITTED_HASH, 'read from Mantle' shown only when live read resolves); on-chain getCommit(1,0)==HERO_CHRONOS_CHAIN hash; lib/wagmi.ts registers mantleSepolia (5003) first in chains[] |
| Three-way canonical-JSON keccak parity (TypeScript == Python == Solidity) is CONFIRMED LIVE ON-CHAIN: calling verify(1,0,<golden canonical bytes>) on the deployed anchor returned recomputed=0xf8aed1ad2a6bcdf567b73fb7fe2814f93d83f7ee1ffdea7d6e70eb663f55b82a, byte-identical to the TS and Python keccak of the same receipt. | `LIVE_ONCHAIN` | eth_call verify() to 0xB0319b2e... on rpc.sepolia.mantle.xyz returns recomputed==golden keccak; kit/test/receipt.test.ts GOLDEN_KECCAK; agents/tests/test_receipt.py golden; independent Python recompute == golden (True) |
| GlassBoxRegistry is deployed and live on Mantle Sepolia (5003) at 0x52237944151D385222316f446b7a08Cde44b6797. It lets external agents implementing IGlassBoxAgent register (agentId<->contract) for the leaderboard, guarded by try IGlassBoxAgent(addr).agentId(). | `LIVE_ONCHAIN` | eth_getCode bytecode present; broadcast/Deploy.s.sol/5003/run-latest.json name->0x5223...6797; src/GlassBoxRegistry.sol; 4 passing tests |
| RoundState contract is deployed and live on Mantle Sepolia (5003) at 0xe016C12d1D42cc2E4ECaaCE2B0fd5058cC984Ea5 (per-round Open->Pending->Settled lifecycle, settler-gated). The CONTRACT is live; no round has been opened on-chain. | `LIVE_ONCHAIN` | eth_getCode bytecode present; broadcast run-latest.json name->0xe016...4Ea5; my cast call roundsCount()=0 on chain 5003; src/RoundState.sol; 8 passing tests |
| ReasoningRepToken is deployed and live on Mantle Sepolia (5003) at 0x72eA3147F126c9F1C797D1E56D8cF65cFA3d69F9. Its on-chain identity is name 'Glass-Box Reasoning Reputation', symbol GBRR, decimals 0 — a non-transferable reputation token (transfer/approve/transferFrom revert NonTransferable) with combine() 10:1 tier rollups. | `LIVE_ONCHAIN` | eth_getCode bytecode present; my cast call name()='Glass-Box Reasoning Reputation', symbol()='GBRR' on chain 5003; broadcast run-latest.json ReasoningRepToken->0x72ea...69f9; 9 passing tests |
| HumanArena CONTRACT is deployed and live on Mantle Sepolia (5003) at 0x51eaD31AdA817281bD853a8ab9a011b1BFcAdf99. It exposes submitCall(direction,conviction,reasoningHash) on an open round (no capital), a shared pure score() rule, and beatAgent() view; constructor reverts if roundState has no code. | `LIVE_ONCHAIN` | eth_getCode bytecode present; broadcast run-latest.json HumanArena->0x51ea...df99; src/HumanArena.sol; 14 passing tests |
| All 5 Glass-Box project contracts are deployed on Mantle SEPOLIA (chain 5003 = 0x138b) only. NONE are deployed on Mantle MAINNET (chain 5000); eth_getCode returns 0x there. | `LIVE_ONCHAIN` | my cast chain-id=5003; eth_getCode bytecode present for all 5 on 5003; my cast code on anchor over rpc.mantle.xyz (chain-id 5000) returns 0x; broadcast has exactly 5 CREATE txns on chain 5003 |
| The ERC-8004 Identity (0x8004A169...) and Reputation (0x8004BAa1...) registries genuinely exist on-chain on both Mantle chains, but they are the CANONICAL registries deployed by the erc-8004 team via CREATE2. The project did NOT deploy them; the honest verb is the project INTEGRATES WITH / BINDS TO them. | `LIVE_ONCHAIN` | frontend/lib/contracts.ts L1-3; CONTRACTS.md 'canonical, deployed by erc-8004 team via CREATE2'; no deploy in script/Deploy.s.sol; kit/src/erc8004.ts ships only addresses+types |
| The Fold ensemble computes signal = sum(signal_i*conf_i)/sum(conf_i) (confidence-weighted arithmetic mean, clamped to [-1,1]) and confidence = (c_chronos*c_da*c_web*c_mood)**0.25 (geometric mean of the four confidences); returns (0,0) when confidence sum is 0. | `BUILT_TESTED` | agents/shared/ensemble.py L75-106 (L103 signal, L104 confidence); test_fold_is_confidence_weighted_mean and test_confidence_is_geometric_mean in agents/tests/test_fold.py; kit/src/agent.ts L89-115 parallel foldEnsemble |
| Honest backtest, independently reproduced: the Fold beats the AVERAGE single agent's Sharpe in 200/200 seeds and has shallower drawdown than the WORST single agent in 200/200 seeds. This is variance reduction only — the code EXPLICITLY disclaims beating a plain mean, and on direction the Fold TIES a plain mean (sample seed 42: Fold Sharpe 5.295 vs equal-weight baseline 5.329, Fold slightly LOWER). Results use synthetic agent outputs over GBM bars, not live LLM/market data. | `BUILT_TESTED` | I ran run_backtest seeds 0-199 (days=180,edge=0.15): 'fold_sharpe>avg_single: 200/200' and 'fold_dd>=worst_single: 200/200'; seed=42 fold 5.295 vs baseline 5.329; ensemble.py docstring L84-91 'We make NO claim that this beats a plain mean'; test_ensemble_beats_average_single_agent |
| On-chain reasoning receipt is deterministic: canonical_receipt = sorted-key compact UTF-8 JSON of the ReasoningChain; reasoning_hash = Ethereum keccak256 (eth_utils.keccak, NOT FIPS sha3). A regression test guards against the sha3 mistake. Golden vector reproduced exactly: 0xf8aed1ad2a6bcdf567b73fb7fe2814f93d83f7ee1ffdea7d6e70eb663f55b82a. | `BUILT_TESTED` | agents/shared/base.py L91-110; test_reasoning_hash_is_keccak_not_fips_sha3; kit/src/receipt.ts canonicalReceipt/receiptHash; independent keccak recompute == golden (True) |
| 148 automated tests pass across three independent stacks: 59 Solidity/Foundry (incl fuzz), 74 Python/pytest (test_backtest 16 / test_fold 8 / test_live 17 / test_provenance 10 / test_receipt 7 / test_settler 11 / test_share 5; require repo root on PYTHONPATH; test_live's ABI test needs contracts/out built via `forge build` — it skips with a clear reason on a fresh clone), 15 TypeScript/vitest (receipt 10 + arena 5). 0 failures across all suites. | `BUILT_TESTED` | forge test '59 tests passed, 0 failed, 0 skipped' (8 suites); PYTHONPATH=. pytest agents '74 passed'; vitest 'Tests 15 passed' |
| AgentExecutor.sol risk engine is written and tested but NOT deployed: routes a Fold ensemble decision to a swap via ISwapRouter with hard caps MAX_TRADE_BPS=500 (5%), DRAWDOWN_LIMIT_BPS=2000 (20% halt), MIN_CONFIDENCE_BPS=5000 (50% floor), and Devil's-Advocate veto. It is absent from Deploy.s.sol and the chain-5003 broadcast. | `BUILT_TESTED` | src/AgentExecutor.sol; script/Deploy.s.sol L17-18 (deploys separately) + L37-41 (only 5 contracts); broadcast run-latest.json CREATE list excludes AgentExecutor; 10 passing tests |
| MerchantMoeAdapter.sol is written and tested but NOT deployed: wraps AgentExecutor's simple ISwapRouter into Merchant Moe Liquidity Book (LBRouter) path-based calls. Absent from Deploy.s.sol and the broadcast. | `BUILT_TESTED` | src/MerchantMoeAdapter.sol; script/Deploy.s.sol L17-18; broadcast run-latest.json absent from CREATE list; 6 passing tests |
| glassbox-agent-kit is a publish-ready TypeScript/npm package (v0.1.0, ESM, viem ^2.21) whose single entry re-exports 7 modules; its load-bearing core (canonicalReceipt, receiptHash, foldEnsemble, parseProvenance/isFullyLive, arenaScore/scoreDecision/beats) is covered by 15 passing tests, and `npm run build` emits a dist/ that exposes the full API (incl arena.js + parseProvenance/isFullyLive/arenaScore in dist/index.js) so `import {...} from "glassbox-agent-kit"` resolves. The on-chain helpers (commitReasoning/verifyReasoning) and GlassBoxAgent.decide run end-to-end offline but are NOT in the automated test suite. | `BUILT_TESTED` | kit/package.json (main/exports -> ./dist), kit/src/index.ts; kit/test/receipt.test.ts (10) + kit/test/arena.test.ts (5); ran `npm run build` -> dist/arena.js emitted, grep dist/index.js shows arenaScore/scoreDecision/beats/parseProvenance/isFullyLive; commitReasoning/verifyReasoning have zero test coverage (verify() decode validated manually on-chain only) |
| A third-party agent can plug into the kit with NO internal code: kit/examples/foreign-agent.ts (a funding-skew frame, agentId 8242) imports ONLY glassbox-agent-kit and produces a provenance-bearing receipt anyone can CHECK (local recompute + parseProvenance/isFullyLive) and BEAT (arenaScore, a mirror of on-chain HumanArena.score()). It runs offline via `npm run example:foreign`. | `BUILT_TESTED` | kit/src/arena.ts arenaScore/scoreDecision/beats; kit/examples/foreign-agent.ts (static imports resolve only to ../src; ran and printed receipt+provenance+tamper+beat); kit/test/arena.test.ts (5 tests) incl negative-remainder vector arenaScore(-1,3333,100)=-33 shared with contracts/test/HumanArena.t.sol score() (forge-verified -33, truncate-toward-zero) |
| Settler risk gate (Devil's-Advocate veto, 50% confidence floor, weak-signal block, 5% size cap) and share-card/tweet copy are implemented and tested in dry_run mode (no on-chain dependency exercised). | `BUILT_TESTED` | agents/tests/test_settler.py (11 tests, dry_run=True); agents/tests/test_share.py (5 tests) |
| Mainnet deploy cost is an ESTIMATE ≈0.13 MNT (~$0.07) + ~$0.004/commit, derived from real inputs: gasUsed summed from the actual Sepolia deploy receipts (2,608,439 across 5 CREATEs), live mainnet eth_gasPrice (50.0001 gwei) + GasPriceOracle DA fees + MNT/USD ($0.54) read 2026-06-10, and eth_estimateGas for commit() against the live Sepolia anchor. NO mainnet deploy has been performed — never present these as measured mainnet costs. | `BUILT_TESTED` | contracts/broadcast/Deploy.s.sol/5003/run-latest.json gasUsed fields; my JSON-RPC reads of rpc.mantle.xyz (eth_gasPrice, tokenRatio=3052, getL1Fee) and coins.llama.fi MNT price on 2026-06-10; docs/mainnet-runbook.md cost table discloses the same provenance |
| Nansen and Elfa integrations are wired and real-API-ready (real httpx GET to api.nansen.ai / api.elfa.ai when the API key is set) but are NOT live data sources in this build: keys are UNSET, so both return deterministic mock data tagged '_mock': True. Likewise DeepSeek is the intended live LLM backend (orchestrator raises ValueError without a key) but was never exercised here. | `BUILT_TESTED` | agents/shared/tools.py L22-85 (key gates + _mock_* helpers); ran nansen_smart_money_flows/elfa_sentiment -> '_mock':True; agents/orchestrator/main.py L43-52 raises ValueError; env keys UNSET; backtest uses synthetic_agent_outputs not DeepSeek |
| Each reasoning receipt commits the LIVENESS PROVENANCE of every input inside the existing data_sources field (nansen:live / nansen:mock / nansen-wallets:mock / defillama:unavailable / mantle-rpc:live@block=N / peers:internal), stamped at the data's ORIGIN so a key-present-but-call-failed run is provably mock. Flipping a liveness tag changes the keccak — a receipt cannot claim mock data was live. Provenance rides the EXISTING field, so canonical bytes / the on-chain commit value are unchanged (the live Sepolia commits predate it and carry legacy bare data_sources). | `BUILT_TESTED` | agents/shared/tools.py _prov/collect_provenance, origin-stamped on live + _mock_* paths; agents/shared/base.py data_sources=sorted(set(provenance)); agents/tests/test_provenance.py test_provenance_is_committed_and_tamper_evident (live!=mock keccak), test_mock_path_is_stamped_mock_at_origin, test_each_agent_declares_provenance_in_mock_mode; 2nd golden vector test_provenance_format_keccak_golden=0xdad4f919...cee33e (py) == kit/test/receipt.test.ts GOLDEN_KECCAK_V2; kit exports parseProvenance/isFullyLive |
| A first-party on-chain source (mantle_rpc_activity) reads recent V2 Swap logs straight off our OWN Mantle RPC, tagged mantle-rpc:live@block=N — structurally unmockable (no third party in the path). MANTLE_RPC_URL is UNSET in this build, so it honestly returns mantle-rpc:unavailable (never silently mock), never crashes the round, and is wired into Chronos. | `BUILT_TESTED` | agents/shared/tools.py mantle_rpc_activity (MANTLE_RPC_URL read at call-time; broad except -> unavailable); agents/chronos/agent.py gather_context; agents/tests/test_provenance.py test_mantle_rpc_declares_unavailable_without_endpoint; env MANTLE_RPC_URL unset |
| The PnL grading LOOP is implemented and tested off-chain: SettlerService.settle_round reads each agent's ALREADY-COMMITTED directional_signal off the round record and grades it via resolve_outcome (independent price move in bps) + score_prediction (rewards matched direction, scales with conviction) — recomputable, not circular. A live price feed (mantle_spot_price) and a settle CLI (live.py settle --live, incl on-chain RoundState.settle) now EXIST and are tested offline, but NO live settlement has ever executed: it is still manually invoked, and roundsCount()=0 on every chain. | `BUILT_TESTED` | agents/settler/service.py settle_round; agents/settler/live.py settle_from_record (refuses non-live prices); agents/tests/test_live.py test_settle_from_record_grades_committed_signals, test_settle_refuses_unavailable_settlement_price; agents/tests/test_provenance.py (3 settle tests); roundsCount()=0 on 5003 |
| The live-runner composition root EXISTS and is tested offline (agents/settler/live.py): signing web3-v7 client (SignAndSendRawMiddleware + default_account), contract handles from the compiled Foundry ABIs with an on-chain code-presence check (wrong-chain/stale-address config fails fast), chain-seeded decision indices (probes the anchor; transport errors PROPAGATE rather than reading as a free slot), round-start price capture persisted BEFORE the first tx (inflight_*.json — a mid-flight crash cannot orphan an ungradeable round), collision-proof never-overwrite round records (dry_round_/round_ namespaces), and a settle command that grades ALREADY-COMMITTED signals and refuses dry-run records, re-settlement, and non-live prices. --live has NEVER been executed against any chain (no funded settler key in this build): the on-chain TX branches (transact/settle) have ZERO test execution — coverage there is ABI name/signature cross-checks plus the refusal gates, nothing more. Three latent live-path bugs were found and fixed in service.py (web3 v7 .w3 rename, missing receipt waits/signing, 0x-prefix hash defense). | `BUILT_TESTED` | agents/settler/live.py; agents/tests/test_live.py (17 tests: ABI exposure, signer wiring, index seeding + transport-error propagation, record round-trip, overwrite/collision/inflight/re-settle/dry-run-record refusal gates); service.py shim diffs; --live never executed (no key in env) |
| A settlement-grade price source EXISTS with origin-stamped provenance: mantle_spot_price reads the keyless DeFiLlama coins API for the pair legs, tags defillama-price:live@ts=N with the OLDER observation timestamp, and declares defillama-price:unavailable on ANY failure (unknown pair, HTTP error, confidence<0.9) — it never returns a mock price, because a grade computed on invented prices would be a fabricated outcome. THE FUNCTION ITSELF ran against the live endpoint 2026-06-10: mantle_spot_price('mETH/USDC') returned available=True, price 1807.34, provenance 'defillama-price:live@ts=1781107126'. | `BUILT_TESTED` | agents/shared/tools.py mantle_spot_price/_parse_llama_pair; agents/tests/test_live.py (4 price tests incl dead-endpoint + low-confidence refusal); my live run of the function from repo root (output above) + earlier curl showing all 5 tokens at confidence 0.99 |
| The deployed reputation token is the live-on-Sepolia GBRR contract; the frontend rename 'Sovereign Reasoning Coin' and the export symbol SOVEREIGN_REASONING_COIN_MAINNET are a UI label, not the on-chain identity. The '_MAINNET' suffix on all Glass-Box exports is misleading — the values are the Sepolia (5003) addresses (file header says so). | `LIVE_ONCHAIN` | frontend/lib/contracts.ts L5-11 (comment 'LIVE on Mantle Sepolia (chain 5003)' on *_MAINNET-named consts); my on-chain name()='Glass-Box Reasoning Reputation'/symbol()='GBRR'; broadcast ReasoningRepToken->0x72ea...69f9 |
| The full four-agent anchor and the PnL->reputation settlement flow are DESIGNED/post-hackathon, not live. Only Chronos's commits (exactly 2) plus the in-browser tamper test run on-chain today; RoundState has 0 rounds opened, so no settlement has executed. | `DESIGNED_PENDING` | my cast commitsCount()=2 and roundsCount()=0 on chain 5003; getCommit(2,0)/(3,0)/(4,0) revert 'no commit'; README future-tense 'four-agent anchor lands with mainnet'; agent-architecture.md L203 settle is a 'worked example' |
| The AI-vs-Human arena ROUND is simulated in the UI: HumanCall.lockIn() calls resolveDemo() against a hardcoded DEMO_ROUND (roundId 17, realizedPnlBps -120) and never invokes the deployed HumanArena.submitCall; the UI permanently stamps 'SIMULATED ROUND'. The contract is deployed but the round/grading is not on-chain. | `DESIGNED_PENDING` | HumanCall.tsx L26-31 resolveDemo, L45 'SIMULATED ROUND' badge always shown; lib/arena.ts DEMO_ROUND L24-36; humanArenaAbi exists but is never wired in HumanCall; my roundsCount()=0 |
| The agent-identity binding to ERC-8004 is design-only: AGENT_IDS = {chronos:0, devils_advocate:0, web:0, mood:0} with comment 'set after Day 4 mints on mainnet'. No agent-identity NFT mint is referenced, read, or confirmed. | `DESIGNED_PENDING` | frontend/lib/contracts.ts L13-19 (AGENT_IDS all 0); no mint/identity read in frontend or SDK; CONTRACTS.md 'Agent Identity NFTs (4 mints on mainnet, Day 2)' unchecked |
| The Reasoning Stream and Leaderboard show NO live on-chain feed when isLive is true — demo fixtures are suppressed (isLive ? [] : DEMO_*), not replaced by an on-chain source; the live site renders 'Waiting for first signal...' and an empty leaderboard. | `DESIGNED_PENDING` | page.tsx L142 Leaderboard rows={isLive ? [] : DEMO_LEADERBOARD}, L145 ReasoningStream steps={isLive ? [] : DEMO_STEPS}; live HTML grep: 'Waiting for first signal' x1, demo rows x0 |
| glassbox-agent-kit is NOT published to any registry — npm returns 404 and no PyPI/pip artifact exists. The Python parity code is in-repo reference (agents/shared/base.py), not a distributable package. package.json is publish-ready but the artifact lives only in-repo. | `DESIGNED_PENDING` | registry.npmjs.org/glassbox-agent-kit -> {'error':'Not found'}; npm view -> E404; pip N/A for a TS package |
| Outreach drift to fix before sending: docs/outreach/mantle-devrel-pitch.md asserts forbidden claims in ship-ready template form — 'Agent Identity NFTs just minted on Mantle mainnet' with tx placeholders, and present-tense 'ERC-8004 reputation graded by realized on-chain PnL'. These contradict ground truth (no mainnet deploy, AGENT_IDS=0, settlement not live) and must be hedged/removed. | `DESIGNED_PENDING` | docs/outreach/mantle-devrel-pitch.md L3,16,27-32,41; contradicts frontend/lib/contracts.ts L14-19 (AGENT_IDS=0) and ground-truth roundsCount=0 / mainnet code=0x |
| Doc to align before integrators rely on it: docs/integration-spec/SCHEMA.md's example reasoning chain uses camelCase (agentId/promptTokens/dataSources) and URI-style sources (nansen://..., elfa://...), which CONTRADICT the real canonical receipt — snake_case (agent_id/prompt_tokens/data_sources) with source:mode[@ref] provenance tags. A team integrating against the doc as written would never surface liveness. Align SCHEMA.md to the kit/src/receipt.ts shape. | `DESIGNED_PENDING` | docs/integration-spec/SCHEMA.md L97/L108 vs agents/shared/types.py ReasoningChain + kit/src/receipt.ts canonicalReceipt; provenance format per agents/shared/tools.py _prov |
| DO NOT claim the system beats the market, generates alpha, or beats a plain/simple mean — the code explicitly disclaims this and the measured edge is variance reduction only; on direction the Fold TIES a plain mean (sample Fold Sharpe 5.295 < baseline 5.329). | `DO_NOT_CLAIM` | agents/shared/ensemble.py docstring L84-91; docs/agent-architecture.md L43,45,238; README.md L15; my run seed=42 Fold 5.295 vs baseline 5.329 |
| DO NOT claim any project contract is deployed/live on Mantle MAINNET (chain 5000) — all 5 are Sepolia-only; mainnet eth_getCode is 0x. | `DO_NOT_CLAIM` | my cast code on chain 5000 returns 0x for the anchor; broadcast CREATE txns are all chain 5003; README 'mainnet settlement deploy is post-hackathon' |
| DO NOT claim a live four-agent anchor or more than 2 on-chain commits — commitsCount() is exactly 2, both Chronos (agentId 1); getCommit for agents 2/3/4 reverts 'no commit'. | `DO_NOT_CLAIM` | my cast commitsCount()=2; getCommit(2,0)/(3,0)/(4,0) revert 'no commit' on chain 5003 |
| DO NOT claim AgentExecutor.sol (or its risk caps / drawdown halt / Merchant Moe swap) is deployed, verified, or executing trades — it is written+tested only, absent from the deploy broadcast. | `DO_NOT_CLAIM` | broadcast run-latest.json has no AgentExecutor CREATE; only 5 contracts deployed on 5003; agent-architecture.md §7 uses live voice but is a worked example |
| DO NOT claim agent-identity NFTs are minted or that ERC-8004 ids are bound — AGENT_IDS are all 0; binding is design-only. Also DO NOT claim the project deployed the ERC-8004 registries — it only integrates with / binds to the canonical erc-8004 CREATE2 deploys. | `DO_NOT_CLAIM` | frontend/lib/contracts.ts L13-19 (AGENT_IDS=0, 'set after Day 4 mints'); CONTRACTS.md 'canonical, deployed by erc-8004 team'; no mint in repo |
| DO NOT present the AI-vs-Human arena round or its PnL grading as real/settled/on-chain — the round is SIMULATED (resolveDemo against a hardcoded DEMO_ROUND); the HumanArena contract being deployed does not make the round real. DO NOT claim PnL->reputation settlement runs — it is design, gated to a future mainnet deploy. | `DO_NOT_CLAIM` | HumanCall.tsx L45 'SIMULATED ROUND', L49 'no settled on-chain round yet'; submitCall never invoked; my roundsCount()=0; README.md L19,49 'mainnet settlement deploy is post-hackathon' |
| DO NOT claim Nansen, Elfa, or DeepSeek are live/verified data sources in this build — Nansen/Elfa return deterministic mock ('_mock':True) with keys unset, and DeepSeek was never exercised. DO NOT cite the 'deepseek-v4-pro' model id as verified-live — it is unconfirmed and inconsistent with the canonical 'deepseek-reasoner' id used in the golden test. | `DO_NOT_CLAIM` | agents/shared/tools.py key gates; ran tools -> '_mock':True; DEEPSEEK_API_KEY unset; base.py L37 'deepseek-v4-pro' vs test_receipt.py L26 model='deepseek-reasoner'; .env.example comment now says 'NOT verified live in this build' (the old aspirational 'verified live 2026-06-07' text was removed 2026-06-10) |
| DO NOT assert the contracts are 'verified on Mantle Explorer' (source published) — this is separate from deployment and was NOT independently confirmed; tag TO-CONFIRM, never assert. The UI only links to mantlescan address pages, which does not prove source verification. | `DO_NOT_CLAIM` | no explorer verification API queried; VerifyPanel/page.tsx link to sepolia.mantlescan.xyz/address/<addr> (a UI link only); no source-verification assertion in pitch docs |
| DO NOT present the backtest as proof of alpha or as the headline, and DO NOT present the 200/200 result as live-trading or real-LLM performance — it is supplementary evidence produced by synthetic agent outputs over synthetic GBM bars. The headline is the verifiable pre-committed audit trail, which proves provenance, not correctness. | `DO_NOT_CLAIM` | docs/alpha-data-pitch.md:24 'backtest is supplementary evidence, not the headline' and 'proves provenance, not correctness'; backtest harness.py uses synthetic_agent_outputs (edge=0.15) over GBM |

| DO NOT claim provenance proves the data was CORRECT or that the reasoning/trade was right — provenance proves only the SOURCE and LIVENESS of each input (live/mock/unavailable) and that it can't be silently faked. It is integrity of the record, not correctness of the decision. | `DO_NOT_CLAIM` | agents/shared/tools.py _prov tags source:mode only; test_provenance.py asserts tamper-evidence of liveness, not decision correctness |
| DO NOT claim PnL/reputation settlement (settle_round) is LIVE, on-chain, or auto-running — the price feed and settle CLI now exist (tested offline), but settlement remains manually invoked and has NEVER executed against any chain (roundsCount()=0). DO NOT claim mantle-rpc is a LIVE data source feeding decisions in this build — MANTLE_RPC_URL is unset, so it is mantle-rpc:unavailable. | `DO_NOT_CLAIM` | agents/settler/live.py settle is a manual CLI, --live never run (no funded key); pytest env MANTLE_RPC_URL unset -> unavailable; roundsCount()=0 on chain 5003 |
| DO NOT claim a third-party TEAM has plugged in, adopted the SDK, or that the foreign agent COMMITTED on-chain — kit/examples/foreign-agent.ts is self-authored (it proves the kit is a SUFFICIENT standalone integration surface, not that an external team adopted it) and its commit is gated on PRIVATE_KEY (not sent). The honest claim is "the kit is a sufficient integration surface; a foreign agent needs nothing but the published package." | `DO_NOT_CLAIM` | kit/examples/foreign-agent.ts authored in-repo; commit path requires PRIVATE_KEY (unset) -> prints, does not send; roundsCount()=0 on chain 5003 |
## Banned phrasings (never say)
- beats the market
- beats a plain mean
- beats a simple mean
- generates alpha
- predictive on its own
- four-agent anchor (as live)
- four reasoning hashes committed on-chain
- deployed on Mantle mainnet
- live on mainnet
- agent-identity NFTs minted
- NFTs just minted on Mantle mainnet
- ERC-8004 ids bound
- verified on Mantle Explorer
- source-verified on Mantlescan
- Sovereign Reasoning Coin (as on-chain identity)
- Nansen live data
- Elfa live sentiment
- DeepSeek verified live
- deepseek-v4-pro verified
- arena round is real
- settled on-chain round
- PnL->reputation settlement is live
- project deployed the ERC-8004 registries
- SDK published to npm
- backtest proves alpha
- real-LLM backtest performance
- more than 2 on-chain commits
- proves the trade was correct
- provenance proves correctness
- live settlement scoring
- reputation graded live
- mantle-rpc live data (this build)
- third-party team plugged in
- external team adopted the SDK
- foreign agent committed on-chain
