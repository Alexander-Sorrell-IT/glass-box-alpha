# BUIDL edit pack — paste into DoraHacks Manage Submission (editable until the deadline)

> Each block is ledger-traced. Apply top to bottom; 10 minutes total.

## 1. Confirm the second track (do this first)
The card shows "1 of 2" tracks. Verify **Alpha & Data (Mirana Ventures)** is the
second nomination alongside AI Trading & Strategy. If the slot is empty, add it —
it doubles the prize surface at zero cost.

## 2. Append to DETAILS — business potential (the only scored dimension with zero copy today)

### Who pays, and the path there
The receipt primitive is audit-trail-as-a-service for the agent economy: platforms
running AI agents that move money need to prove a decision was not rewritten after
the fact. The SDK stays free and open (distribution); the business is hosted
anchoring, verification dashboards, and compliance reporting for agent platforms
and funds whose LPs demand exactly this record. Post-hackathon path: Mantle
ecosystem grants (verifiability as public-good infrastructure), then 1-2 design-
partner integrations via the open broadcast schema — the kit's foreign-agent
example is the integration template.

## 3. Append to DETAILS — data liveness update (replaces nothing, adds a truth)

Update (Jun 10): the Nansen integration is now verified LIVE — real smart-money
netflow on Mantle via the production API, with the receipt committing
`nansen:live` provenance inside the hash. The provenance system now demonstrates
its live-vs-mock distinction with real data: a receipt cannot claim mock data was
live, and flipping one tag turns the on-chain hash red.

## 4. One-line edit — explorer verification (now true, previously unclaimable)
Where DETAILS says "Live on Mantle Sepolia (chain 5003, bytecode confirmed on-chain)",
change to: "Live on Mantle Sepolia (chain 5003) — all 5 contracts source-verified
on Mantle Explorer (Exact Match)".

## 5. Hold for the live round (do NOT paste until it actually happens)
After the first settled `--live` round: add the round record link + "decisions AND
outcomes recorded on Mantle: open round, 4 reasoning commits, on-chain settlement
graded against an independent price read". After mainnet deploy: add the mainnet
addresses table. Re-verify each claim against the chain before pasting (ledger rule).
